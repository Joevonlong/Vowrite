{
  "description": "Vowrite product repo hooks — delegates to the workspace shared branch guard when present; standalone clones fall back to .githooks/pre-commit.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "G=\"$(git rev-parse --show-toplevel)/../.agents/hooks/codex-branch-guard.sh\"; if [ -x \"$G\" ]; then bash \"$G\"; else exit 0; fi",
            "statusMessage": "Branch guard (Task Initiation Protocol)"
          }
        ]
      }
    ]
  }
}
