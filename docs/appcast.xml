<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Vowrite Updates</title>
    <link>https://vowrite.com/appcast.xml</link>
    <description>Vowrite release feed</description>
    <language>en</language>
    <!--
      Managed by release.sh — do not edit manually.
      Each release: sign_update signs the DMG, release.sh inserts the <item>.
    -->
    <item>
      <title>Vowrite 0.2.1.1</title>
      <description><![CDATA[<h2>What's New in 0.2.1.1</h2><ul></ul><h3>### Added</h3><ul><li><b>VowriteKitTests target</b>: First automated test target. Six XCTest files covering `Mode` Codable roundtrip + F-063 backward-compat decode, `ModeConfig` field mapping and `withStyleOverride` behaviour, `OutputStyle` Codable roundtrip, `ReplacementRule` Codable roundtrip + ID uniqueness, builtin data integrity (Clean mode UUID stability, builtin shortcut index uniqueness, `OutputStyle.noneId` resolution), and the empty-rules early-return path of `ReplacementManager.apply`. `ops/scripts/test.sh` now invokes `swift test` between the Kit build and the Mac build.</li></ul><h3>### Changed</h3><ul><li><b>WindowHelper</b>: dropped redundant `openSettings()` / `openHistory()` wrappers that only delegated to `openMainWindow()`. Five call sites (`VowriteApp` Settings command, `MenuBarView` History/Settings buttons, `VowriteMenuView` Settings/History entries) now call `openMainWindow()` directly.</li></ul><h3>### Fixed</h3><ul><li><b>ModelContainer init failure no longer crashes the app</b>: `AppState.init` on Mac and iOS now falls back to an in-memory store and surfaces a "history temporarily unavailable" banner in `HistoryView` instead of `fatalError`. iOS banner additionally calls out that keyboard extension records are also affected because the keyboard extension reads the App Group SwiftData store directly.</li><li><b>About page shows the real app icon</b> instead of the SF Symbol `mic.circle.fill` placeholder. Pulled directly from `NSApp.applicationIconImage` so it always matches the bundled `AppIcon.icns`. `build.sh` icon regeneration logic also tightened: `.icns` is now rebuilt whenever `Resources/AppIcon-source.png` is newer than the existing `.icns` (previously only ran on first generation), so brand refreshes propagate without a manual clean.</li></ul><h3>### Removed</h3><ul><li><b>MiniMax OAuth flow</b>: removed `MiniMaxOAuthService`, `MiniMaxOAuthCard`, and the `minimax_intl`/`minimax_cn` cases in `OAuthRefreshManager.performRefresh`. `clientID` was never obtained from MiniMax developer support, so the flow was never functional. `MiniMaxOAuthCard` was also a 100% orphan (zero instantiation sites). `PresentationContextBridge` was extracted to its own file because `OpenAICodexOAuthService` still uses it. New `MiniMaxOAuthPurge.runIfNeeded()` one-shot clears stale OAuth tokens and `auth.method.minimax_*` keys on next launch.</li></ul>]]></description>
      <pubDate>Sat, 02 May 2026 03:56:22 +0200</pubDate>
      <sparkle:version>7</sparkle:version>
      <sparkle:shortVersionString>0.2.1.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.0</sparkle:minimumSystemVersion>
      <enclosure
        url="https://github.com/Joevonlong/Vowrite/releases/download/v0.2.1.1/Vowrite-v0.2.1.1.dmg"
        type="application/octet-stream"
        sparkle:edSignature="SHjPv7bYMtkffLOOt3S4f+wnazrlVZUV9uaP0jVqyMWq905E/kekHXK/z8M/fGgYQwDnthEmc9spW997aXOyDQ=="
        length="3185913"
      />
    </item>
  </channel>
</rss>
